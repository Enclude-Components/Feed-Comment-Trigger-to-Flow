minimum_cumulusci_version: '3.90.0'
project:
    name: Feed-Comment-Trigger-to-Flow
    package:
        name: Feed-Comment-Trigger-to-Flow
        api_version: '61.0'
    git:
        default_branch: 'main'
    source_format: sfdx

tasks:
    run_tests:
        options:
            required_org_code_coverage_percent: 75
